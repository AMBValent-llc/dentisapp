import { z } from "zod";
import { prisma } from "@/lib/prisma";
import { requireApiContext } from "@/lib/server-auth";
import { apiError, handleApiError, parseDate } from "@/lib/api";
const schema = z.object({ name: z.string().trim().min(2).max(200).optional(), type: z.string().trim().max(120).nullable().optional(), status: z.enum(["ORDERED", "SCHEDULED", "COMPLETED", "CANCELLED"]).optional(), scheduledAt: z.string().datetime().nullable().optional(), completedAt: z.string().datetime().nullable().optional(), result: z.string().trim().max(5000).nullable().optional(), notes: z.string().trim().max(3000).nullable().optional() });
export async function PATCH(request: Request, { params }: { params: Promise<{ id: string }> }) { try { const context = await requireApiContext(); if (!context) return apiError("No autorizado", 401); const id = (await params).id; const data = schema.parse(await request.json()); if (!await prisma.exam.findFirst({ where: { id, organizationId: context.organization.id } })) return apiError("Examen no encontrado", 404); return Response.json({ exam: await prisma.exam.update({ where: { id }, data: { ...data, scheduledAt: data.scheduledAt === null ? null : parseDate(data.scheduledAt), completedAt: data.completedAt === null ? null : parseDate(data.completedAt) } }) }); } catch (error) { return handleApiError(error); } }
export async function DELETE(_: Request, { params }: { params: Promise<{ id: string }> }) { const context = await requireApiContext(["OWNER", "ADMIN"]); if (!context) return apiError("Se requiere rol administrador", 403); const result = await prisma.exam.deleteMany({ where: { id: (await params).id, organizationId: context.organization.id } }); return result.count ? new Response(null, { status: 204 }) : apiError("Examen no encontrado", 404); }
