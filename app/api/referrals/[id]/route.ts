import { z } from "zod";
import { prisma } from "@/lib/prisma";
import { requireApiContext } from "@/lib/server-auth";
import { apiError, handleApiError, parseDate } from "@/lib/api";
const schema = z.object({ specialty: z.string().trim().min(2).max(160).optional(), provider: z.string().trim().max(160).nullable().optional(), reason: z.string().trim().min(2).max(3000).optional(), status: z.enum(["DRAFT", "SENT", "ACCEPTED", "COMPLETED", "CANCELLED"]).optional(), referredAt: z.string().datetime().nullable().optional(), notes: z.string().trim().max(3000).nullable().optional() });
export async function PATCH(request: Request, { params }: { params: Promise<{ id: string }> }) { try { const context = await requireApiContext(); if (!context) return apiError("No autorizado", 401); const id = (await params).id; const data = schema.parse(await request.json()); if (!await prisma.referral.findFirst({ where: { id, organizationId: context.organization.id } })) return apiError("Remisión no encontrada", 404); return Response.json({ referral: await prisma.referral.update({ where: { id }, data: { ...data, referredAt: data.referredAt === null ? null : parseDate(data.referredAt) } }) }); } catch (error) { return handleApiError(error); } }
export async function DELETE(_: Request, { params }: { params: Promise<{ id: string }> }) { const context = await requireApiContext(["OWNER", "ADMIN"]); if (!context) return apiError("Se requiere rol administrador", 403); const result = await prisma.referral.deleteMany({ where: { id: (await params).id, organizationId: context.organization.id } }); return result.count ? new Response(null, { status: 204 }) : apiError("Remisión no encontrada", 404); }
